<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Book List</title>
    <link rel="stylesheet" href="/styles/mybooks.css" />
  </head>
  <body>
    <%- include("./header.ejs") %>
    <!-- sectino adding books -->
    <section>
      <div class="container">
        <div>
          <h2>Add Book to Review</h2>
          <div class="form-container">
            <form class="form" method="post" action="/addbooks/<%= book._id %>/edit">
              <div class="input-group">
                <label for="bookimage">Image Link</label>
                <input type="text" name="bookimage" id="bookimage" placeholder="" required value="<%= book.image %>" />
              </div>
              <div class="input-group">
                <label for="title">Title</label>
                <input type="text" name="title" id="title" placeholder="" required maxlength="100" value="<%= book.title %>" />
              </div>
              <div class="input-group">
                <label for="authorname">Author Name</label>
                <input type="text" name="authorname" id="authorname" placeholder="" required value="<%= book.author %>" />
              </div>
              <div class="input-group">
                <label for="description">Description</label>
                <textarea name="description" id="description" rows="3" maxlength="1000" minlength="10" required><%= book.description %></textarea>
              </div>
              <button class="btn-book">Update Book</button>
            </form>
          </div>
          <!--  -->
        </div>
        <!--  -->
      </div>
    </section>
  </body>
</html>
